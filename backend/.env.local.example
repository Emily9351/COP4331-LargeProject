# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/your-database-name

# Server Configuration
PORT=80

# Frontend URL (used in password reset emails)
FRONTEND_URL=http://localhost:5173

# Email Configuration (Gmail)
EMAIL_SERVICE=gmail
EMAIL_USER=adnan400283@gmail.com
EMAIL_PASS=idchvrimzownatsa

# Note: This will work locally but NOT on your server (ports blocked)
# For the server, you'll need to either:
# 1. Ask the droplet owner to unblock SMTP ports (587, 465)
# 2. Use SendGrid (recommended - no port blocking issues)
